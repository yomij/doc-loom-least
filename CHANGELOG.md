# Changelog

## Unreleased

- Added first-class `skillshare` installation, update, and verification
  instructions.
- Added `.skillignore` so `skillshare` publishes only canonical Doc Loom Least
  skills from `skills/`, excluding reference skill material under
  `docs/reference/skills/`.
- Added per-skill shared protocol links so `skillshare` `copy` mode works
  without requiring sibling `_shared` directories in targets.
